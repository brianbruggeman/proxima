# plugin — package a `Pipe` others compose

*(builds on: transform)*

Per `examples/README.md`'s status line, `plugin` is listed among the rungs
**not yet built**. `examples/plugin-skeleton/` exists on disk (`Cargo.toml`,
`src/lib.rs`, `tests/composed.rs`) as an early scaffold, but has no
`README.md` teaching prose beside it — including its source here without a
verified lesson to anchor it would be re-typing unfinished work as if it
were a landed rung, which the pattern this book follows deliberately avoids.

This chapter becomes a normal two-include chapter once
`examples/plugin/README.md` + `examples/plugin/main.rs` (or an equivalent
dir-per-example rung) lands.

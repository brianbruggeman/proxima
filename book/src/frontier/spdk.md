# spdk — kernel-bypass storage

*(builds on: dpdk)*

Userspace NVMe: a sans-IO SQE/CQE codec + ring FSM, byte-addressable storage
without the block layer's syscall path. Per `examples/README.md`'s status
line, `spdk` is listed among the rungs **not yet built** — there is no
`examples/spdk/` directory yet, dir-per-example or otherwise, so there is no
verified source for this chapter to include.

This chapter is a placeholder until the rung lands.

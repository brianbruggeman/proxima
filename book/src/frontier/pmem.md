# pmem — persistent memory

*(builds on: transform)*

Byte-addressable, crash-consistent cells, no block layer. Per
`examples/README.md`'s status line, `pmem` is listed among the rungs **not
yet built** — there is no `examples/pmem/` directory yet, so there is no
verified source for this chapter to include.

This chapter is a placeholder until the rung lands.

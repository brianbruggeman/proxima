This book teaches proxima from the same files the example curriculum uses.
Chapters include the actual source under `examples/`, and `mdbook build`
fails if those include paths drift.

The scope-and-sequence below is the literal `examples/README.md` that
drives this book's table of contents, included verbatim. Use the left nav
for chapter links once rendered into the book.

{{#include ../../examples/README.md}}
